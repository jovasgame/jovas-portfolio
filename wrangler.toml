name = "jovas-portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2026-08-03"

# NOTA PARA CLOUDFLARE PAGES:
# El binding PORTFOLIO_KV se vincula desde el Dashboard de Cloudflare:
# Settings > Functions > KV namespace bindings > Binding: PORTFOLIO_KV -> Tu KV Namespace.
